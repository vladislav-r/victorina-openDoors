<body>
    <main class="main container">
      {{> header this.info}}
      <div class="wrapper">
        {{#each data}}
        <div class="wrInner">
          <div class="questionBlock">
            <h5 class="questionBlockTitle">Вопрос №{{this.id}}</h5>
            <p class="blockText">{{this.text}}</p>
         </div>
        <form class="form formAnswers">
          <div class="formInner">
            {{#each this.vars}}
            <div class="formItem">
              <input
                class="formRadio"
                type="radio"
                name="flexRadioDefault"
                value={{this}}
              />
              <label class="formLabel" for="flexRadioDefault1">{{this}}</label>
            </div>
            {{/each}}
          </div>
        </form>
        <div class="correctAnswer d-none">{{this.correct}}</div>    
        </div>
        <hr>   
        {{/each}}
      </div>
        <div class="d-flex justify-content-around w-100">
          <a href="/" class="btn btn-secondary mt-3  f-14 goBackBtn">Назад</a>
          <button class="btn btn-primary mt-3 f-14 checkAnswersBtn">Подтвердить</button>
        </div>
    </main>
    <script src="client/assets/js/historyOfDate.js"></script>
  </body>
</html>
